# Minimal requirements for SmartStudy backend (no OCR)
# Use this if you want to avoid PyTorch/EasyOCR dependencies

# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Document processing
PyPDF2==3.0.1
python-docx==1.1.0
docx2txt==0.8
python-pptx==0.6.23

# Basic image processing (no OCR)
Pillow==10.0.1

# Note: This version supports document uploads but NOT image OCR
# For OCR functionality, use the CPU installation script: ./install-cpu.sh
